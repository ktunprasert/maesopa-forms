version: '3.5'

services:
  pb:
    # image: golang:1.20-alpine
    # entrypoint: /app/pb/entrypoint.sh
    # command: "/app/pb/pocketbase serve --http 0.0.0.0:8090 --publicDir ../sk/build"
    user: ${UID}:${GID}
    image: alpine:latest
    entrypoint: "/app/pb/pocketbase serve --http 0.0.0.0:8069 --publicDir ../sk/build"
    # command: "/app/pb/pocketbase serve --http 0.0.0.0:8090 --publicDir ../sk/build"
    ports:
      - 8090:8090
    expose:
      - 8090
    working_dir: /app/pb
    environment:
      - HOME=/app/pb
    volumes:
      - ${PWD}/pb:/app/pb
      - ${PWD}/sk:/app/sk
      - ${HOME}/go/pkg:/go/pkg
  sk:
    image: node:alpine
    user: ${UID}:${GID}
    # sveltekit build
    command: sh -c "npx pnpm install && npm run build"
    volumes:
      - ${PWD}/sk:/app/sk
    environment:
      - HOME=/app/sk
    working_dir: /app/sk
